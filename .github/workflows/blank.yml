name: CI/CD Pipeline Demo

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Run a continuous integration step
        run: |
          echo "Running automated tests..."
          echo "Code check complete!"
